# Aegis PA 第 02 期接口设计

## 1. 范围与约定

本文定义第 02 期功能的接口契约。第 01 期中的登录态、`conversation_id`、`run_id`、确认项目、幂等键与通用错误响应继续适用。服务端从登录态确定用户和租户，客户端不得提交或覆盖用户标识。

## 3.5 信息查询与来源摘要

| 接口 | 请求数据 | 返回数据 |
|---|---|---|
| `POST /research/queries` | `query`、`source_policy: "official_only"`、可选 `source_run_id` | `research_id`、状态 |
| `GET /research/queries/{research_id}` | 无 | `facts[]`、`inferences[]`、`sources[]`、状态 |
| `GET /research/queries` | 查询历史筛选条件 | 查询摘要列表 |

`facts[]` 必须带 `source_ids[]`；`inferences[]` 必须带前提和不确定性。保存查询词、来源 URL/标题/时间/引用位置、来源事实和推断；网页原文不得被当作工具指令。

## 3.6 受控代码执行

| 接口 | 请求数据 | 返回数据 |
|---|---|---|
| `POST /sandbox/run-drafts` | 语言、代码、输入说明、预期结果、时限、CPU/内存限制 | `draft_id`、资源预览、确认项 |
| `POST /sandbox/run-drafts/{draft_id}/execute` | 服务端确认令牌 | `sandbox_run_id`、状态 |
| `GET /sandbox/runs/{sandbox_run_id}` | 无 | 状态、标准输出、脱敏错误、资源用量、结果文件元数据 |
| `POST /sandbox/runs/{sandbox_run_id}/cancel` | 无 | 取消状态 |

保存运行草稿、确认项、运行状态、输出、错误和资源用量。接口不得接收网络、宿主机路径、内网地址、持久卷或凭据字段。

## 3.9 第三方服务连接管理

| 接口 | 请求数据 | 返回数据 |
|---|---|---|
| `GET /connections` | 无 | 提供方、状态、scope、到期时间、可用工具、最近错误 |
| `POST /connections/{provider}/authorization-url` | 可选请求 scope | OAuth URL、`state` |
| `GET /connections/{provider}/callback` | OAuth `code`、`state` | 授权结果或跳转 |
| `PATCH /connections/{connection_id}` | `enabled`、允许的 scope | 更新后的状态和 scope |
| `DELETE /connections/{connection_id}` | 无 | 断开状态 |

保存连接状态、实际 scope、凭据安全引用、到期时间和授权事件；不返回 access token。扩大 scope 必须重新授权。

## 3.10 审计记录与任务可追溯性

| 接口 | 请求数据 | 返回数据 |
|---|---|---|
| `GET /audit-events` | `from`、`to`、`action`、`run_id` | 审计事件摘要 |
| `GET /audit-events/{audit_event_id}` | 无 | 脱敏详情、关联运行与工具 |
| `GET /runs/{run_id}/trace-summary` | 无 | 消息、步骤、确认、工具、审计关联摘要 |

保存主体、动作、资源、决策、原因、脱敏元数据、时间和运行关联。审计只追加；完整查询要求 `audit.read`。

## 3.11 异常反馈与任务恢复

| 接口 | 请求数据 | 返回数据 |
|---|---|---|
| `GET /runs/{run_id}` | 无 | 步骤状态、已完成/未执行动作、失败原因、补救操作 |
| `POST /runs/{run_id}/retry` | 可选 `from_step` | 新/恢复运行状态、SSE 地址 |
| `POST /approval-items/{approval_item_id}/reject` | 可选 `reason` | 拒绝状态 |

保存 checkpoint、步骤状态、错误码、重试次数、外部副作用幂等键及“结果未知”标记。重试前重新校验权限、工具状态和确认令牌；任何结果未知均返回 `unknown` 或 `failed`。
