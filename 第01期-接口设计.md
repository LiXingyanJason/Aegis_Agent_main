# Aegis PA 一期接口设计

## 1. 文档目的

本文按《第01期-项目预期功能与使用示例》的保留功能编号设计一期接口。每项功能均说明页面需要提交的表单数据、接口、页面可获得的返回数据、需要保存的业务数据和必要控制。

**一期范围：** 3.1～3.4、3.7、3.8。原 3.5、3.6、3.9、3.10、3.11 已迁入《第02期-接口设计.md》。

本文只描述实现功能所需的数据对象与接口契约，不规定数据库类型、表名、SQL、索引或部署方式。服务端从登录态取得当前 `tenant_id`、`user_id`，客户端不得提交或覆盖这两个字段。

## 2. 通用规则

### 2.1 请求、响应与状态

- 基础路径：`/api/v1`；除 OAuth 回调外，所有接口要求登录态。
- 创建、修改、发送、删除、批准、运行代码等写接口要求 `Idempotency-Key` 请求头；同一用户、同一接口、同一幂等键只执行一次。
- 时间使用 ISO 8601 UTC 字符串，页面按用户时区显示。
- 返回的邮件、审计、工具错误和日志必须脱敏；绝不返回 OAuth token、密钥或未授权内容。

成功响应：

```json
{"request_id":"req_01J...","data":{}}
```

失败响应：

```json
{"request_id":"req_01J...","error":{"code":"CONFIRMATION_REQUIRED","message":"该操作需要确认后才能执行。","retryable":false,"details":{}}}
```

常用错误码：`VALIDATION_ERROR`、`UNAUTHORIZED`、`FORBIDDEN`、`NOT_FOUND`、`CONFLICT`、`IDEMPOTENCY_CONFLICT`、`CONFIRMATION_REQUIRED`、`CONFIRMATION_EXPIRED`、`POLICY_BLOCKED`、`TOOL_UNAVAILABLE`、`TOOL_RESULT_UNKNOWN`。

任务状态：`queued`、`running`、`waiting_confirmation`、`completed`、`failed`、`cancelled`。确认项状态：`pending`、`approved_executing`、`executed`、`rejected`、`expired`、`failed`。

### 2.2 确认规则

每个外部副作用动作创建独立 `approval_item`。确认令牌绑定当前用户、动作、预览参数哈希及过期时间；批准一个项目不批准其他项目，令牌只能使用一次。

---

## 3.1 对话任务与进度展示

以下按用户在 `task-console.html` 上的实际操作顺序设计接口。用户不必在进入对话前强制连接第三方服务；任务需要邮箱、日历等能力但尚未授权时，Agent 通过进度事件提示并给出连接入口，授权完成后用户可重新提交或重试。

### 步骤 1：首次进入页面，建立或恢复会话

**页面操作：** 用户打开任务对话页面。前端从 URL 或本地状态取得 `conversation_id`；没有时创建会话，有时加载已有消息与未完成任务。

| 情况 | 接口 | 请求/参数 | 页面使用的返回数据 |
|---|---|---|---|
| 新建会话 | `POST /conversations` | `{ "title": "可选标题" }` | `conversation_id`、标题、状态、创建时间 |
| 恢复会话 | `GET /conversations/{conversation_id}` | 路径参数 | 会话标题、消息列表、关联运行摘要 |
| 恢复未完成任务 | `GET /runs/{run_id}` | 路径参数 | 进度步骤、待确认项目、失败/可重试状态 |

`POST /conversations` 返回示例：

```json
{"data":{"conversation_id":"conv_01J...","title":"新任务","status":"active","created_at":"2026-09-24T01:00:00Z"}}
```

**需要保存：** 会话 ID、标题、状态、创建/最近更新时间；会话与消息、任务运行的关联关系。

### 步骤 2：在底部输入框填写任务并点击“发送”

**页面表单：**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `content` | string | 是 | 用户自然语言任务，1～8,000 字符 |
| `client_message_id` | string | 是 | 前端生成，用于防止重复点击或网络重发 |
| `conversation_id` | path | 是 | 当前会话 ID |

**接口：** `POST /conversations/{conversation_id}/messages`

请求示例：

```json
{"content":"明天下午和王敏约半小时项目同步，并发邮件确认。","client_message_id":"local_msg_01J..."}
```

服务端先保存用户消息，创建任务运行并返回：

```json
{"data":{"message_id":"msg_01J...","run_id":"run_01J...","status":"running","events_url":"/api/v1/runs/run_01J.../events"}}
```

页面将用户文本立即插入对话区，输入框恢复可输入状态，并建立后续 SSE 连接。

**需要保存：** 用户消息（消息 ID、角色、原文安全存储、脱敏展示文本、创建时间）、`client_message_id` 去重关系、任务运行（运行 ID、`request_id`、`trace_id`、状态、输入消息 ID、开始时间）。

### 步骤 3：在对话区查看 Agent 消息、工具预览和右侧执行进度

**接口：** `GET /runs/{run_id}/events`（SSE）。首次连接可带认证信息；断线重连时携带 `Last-Event-ID`，服务端从下一条未接收事件继续发送。

| SSE 事件 | 对话区/右侧页面行为 | `data` 关键字段 | 服务端需要保存 |
|---|---|---|---|
| `assistant_message_delta` | 流式追加 Agent 消息，例如“正在查询双方可用时间” | `message_id`、`delta`、`is_final` | Agent 消息内容、序号、完成状态 |
| `progress_updated` | 右侧增加或更新“输入安全校验通过”“任务路由完成”等步骤 | `step_id`、`label`、`status`、`detail`、`occurred_at` | 任务步骤、状态变更时间、说明 |
| `tool_preview` | 对话区显示工具名、风险等级、查询对象及只读结果 | `tool_call_id`、`tool_name`、`risk_level`、`input_summary`、`output_summary` | 工具名、脱敏输入/输出、状态、时延、错误 |
| `connection_required` | 提示缺少邮箱/日历授权，并提供连接页入口 | `provider`、`required_scopes[]`、`connection_url` | 任务阻塞原因、所需授权范围 |
| `approval_required` | 显示待执行计划和“进入确认页”入口 | `approval_item_ids[]`、`approval_url`、`summary` | 确认项目、预览快照、参数哈希、过期时间 |
| `run_completed` | 展示最终结果，右侧标记完成 | `result_summary`、`status` | 结束状态、最终结果摘要、结束时间 |
| `run_failed` | 展示失败原因和“稍后重试”等操作 | `error_code`、`message`、`retryable` | 错误码、脱敏错误、重试次数、失败时间 |

`tool_preview` 示例：

```json
{
  "type":"tool_preview",
  "data":{
    "tool_call_id":"tool_01J...",
    "tool_name":"calendar.availability.search",
    "risk_level":"read",
    "input_summary":{"attendees":["你","王敏"],"duration_minutes":30},
    "output_summary":{"available_slots":["明日 15:00-15:30"]}
  }
}
```

没有已授权数据源时，服务端发送 `connection_required` 或 `run_completed`（结果状态 `insufficient_data`），页面说明信息不足，不能编造待办或查询结果。

### 步骤 4：涉及外部写入时，通过对话提示进入确认页

**页面操作：** 当 SSE 发送 `approval_required` 时，对话区展示“创建日历事件”“发送确认邮件”等待执行计划和跳转入口。点击入口时，前端跳转 `confirmation.html?run_id={run_id}`。

| 页面动作 | 接口 | 请求/参数 | 页面使用的返回数据 |
|---|---|---|---|
| 加载本次待确认项 | `GET /approval-items?run_id={run_id}&status=pending` | 查询参数 | 每个项目的 ID、动作、风险、预览、状态、过期时间 |
| 查看单项详情 | `GET /approval-items/{approval_item_id}` | 路径参数 | 完整预览、参数版本、核对提示、令牌状态 |

确认项返回应包含可直接渲染的预览，而非让页面再次从草稿拼装：

```json
{
  "data": {
    "approval_item_id": "apv_calendar_01J...",
    "run_id": "run_01J...",
    "action": "calendar.create_event",
    "risk_level": "write",
    "title": "创建项目同步会议",
    "preview": {
      "title": "项目同步",
      "start_at": "2026-09-25T07:00:00Z",
      "end_at": "2026-09-25T07:30:00Z",
      "attendees": ["wangmin@example.com"],
      "calendar_name": "工作日历"
    },
    "status": "pending",
    "expires_at": "2026-09-25T06:30:00Z"
  }
}
```

**需要保存：** 每个待确认动作的 `approval_item_id`、任务 ID、动作类型、风险等级、预览快照、参数哈希、状态、令牌、过期时间和拒绝原因。日历事件和邮件发送必须是两条不同记录。

### 步骤 5：逐项确认外部操作

**页面操作：** 用户在 `confirmation.html` 对单项勾选“我已核对”，点击“批准并创建事件”或“批准并发送邮件”；也可点击“拒绝此项”。

| 页面动作 | 接口 | 请求数据 | 返回数据 |
|---|---|---|---|
| 批准单项 | `POST /approval-items/{approval_item_id}/approve` | `{ "checked": true }` | `execution_status`、关联资源 ID 或执行中的状态 |
| 拒绝单项 | `POST /approval-items/{approval_item_id}/reject` | `{ "reason": "可选说明" }` | `status: "rejected"` |
| 查看批准后的状态 | `GET /approval-items/{approval_item_id}` 或 SSE | 无 | `approved_executing`、`executed` 或 `failed` 及结果 |

批准接口在检查勾选状态、用户身份、项目状态、过期时间和预览参数哈希后，签发一次性 `approval_token` 并执行对应动作。令牌只用于当前项目；项目 A 被批准或拒绝均不改变项目 B 的状态。

**需要保存：** 用户确认决策、决策时间、令牌使用时间、执行状态、外部资源 ID、错误/结果及审计事件。所有写操作使用与该确认项绑定的幂等键。

### 3.1 汇总：核心数据

| 数据对象 | 最少字段 | 用途 |
|---|---|---|
| 会话 | ID、标题、状态、时间 | 承载连续对话 |
| 消息 | ID、会话 ID、角色、内容、时间、运行 ID | 展示用户与 Agent 消息 |
| 任务运行 | ID、状态、步骤、风险、错误、关联标识 | 驱动进度、恢复与追溯 |
| 工具调用 | 工具名、脱敏输入/输出、状态、时延 | 渲染工具预览与排错 |
| 确认项目 | 动作、预览快照、哈希、令牌、状态、过期时间 | 实现逐项批准/拒绝 |
| 审计事件 | 主体、动作、决策、资源、时间 | 追溯写操作与确认 |

---

## 3.2 日程查询与会议安排

以下按用户页面操作示例的路径设计：浏览器先通过 3.1 的 `POST /conversations/{conversation_id}/messages` 创建 `run_id`，再立即连接 `GET /runs/{run_id}/events`（SSE）。此后由服务端 Agent 工作流调用日历工具、生成完整预览，并以事件驱动对话区和右侧进度更新；浏览器不直接调用日历工具。

### 步骤 1：任务中查询双方可用时间

**页面位置：** `task-console.html` 对话区和右侧“执行进度”。页面已建立该 `run_id` 的 SSE 连接，不额外填写日程表单。Agent 先发送“正在查询双方可用时间”的 `assistant_message_delta` 与 `progress_updated`，然后再调用工具。

**服务端内部工具接口，Calendar MCP Server（浏览器不可调用）：** `POST /calendar/availability/search`

```json
{
  "run_id":"run_01J...",
  "attendees":[{"name":"王敏","email":"wangmin@example.com"}],
  "time_range":{"start":"2026-09-25T00:00:00Z","end":"2026-09-26T00:00:00Z"},
  "duration_minutes":30,
  "timezone":"Asia/Shanghai"
}
```

该接口只由 Agent 工作流通过已获授权的日历连接调用。返回内容写入本次运行，再通过 3.1 的 SSE `tool_preview`、`progress_updated` 事件通知页面展示：

```json
{
  "type":"tool_preview",
  "data":{
    "tool_name":"calendar.availability.search",
    "risk_level":"read",
    "input_summary":{"attendees":["你","王敏"]},
    "output_summary":{"available_slots":["明日 15:00-15:30"]}
  }
}
```

右侧进度相应显示“可用时间查询完成”。若未连接日历或缺少读取权限，发送 `connection_required` 或失败事件，不创建日历草稿、不进入确认页。

**需要保存：** 运行 ID、查询参与者、查询时段、时长、时区、脱敏忙闲结果、来源连接、工具调用状态与时间。

### 步骤 2：生成待执行的日历事件预览

**页面位置：** `task-console.html` 对话区。工具返回可用时间后，Agent 选定候选时段（或先向用户澄清），由服务端生成日历草稿和确认项；随后通过 SSE 展示完整预览与“待执行计划：创建日历事件；等待你的决定”。

**服务端内部命令（浏览器不可调用）：** `POST /calendar/event-drafts`

```json
{
  "run_id":"run_01J...",
  "title":"项目同步",
  "start_at":"2026-09-25T07:00:00Z",
  "end_at":"2026-09-25T07:30:00Z",
  "timezone":"Asia/Shanghai",
  "attendees":[{"name":"王敏","email":"wangmin@example.com"}],
  "calendar_id":"work_calendar",
  "description":"项目同步会议"
}
```

服务端保存日历草稿，冻结可供用户核对的预览，并创建独立确认项目：

```json
{
  "data":{
    "draft_id":"cal_draft_01J...",
    "approval_item_id":"apv_calendar_01J...",
    "status":"pending",
    "preview":{
      "title":"项目同步",
      "start_at":"2026-09-25T07:00:00Z",
      "end_at":"2026-09-25T07:30:00Z",
      "timezone":"Asia/Shanghai",
      "attendees":["wangmin@example.com"],
      "calendar_name":"工作日历"
    },
    "expires_at":"2026-09-25T06:30:00Z"
  }
}
```

服务端按顺序向对话 SSE 发送：

1. `assistant_message_delta`：说明已找到候选时间并生成待执行计划；
2. `plan_preview`：携带会议主题、参与者、时间、工作日历等完整预览，供对话区直接显示；
3. `approval_required`：携带 `run_id`、`approval_item_id` 和确认页地址，供用户进入确认页。

`plan_preview` 事件示例：

```json
{"type":"plan_preview","data":{"approval_item_id":"apv_calendar_01J...","action":"calendar.create_event","risk_level":"write","preview":{"title":"项目同步","start_at":"2026-09-25T07:00:00Z","end_at":"2026-09-25T07:30:00Z","timezone":"Asia/Shanghai","attendees":["wangmin@example.com"],"calendar_name":"工作日历"}}}
```

此时不调用第三方日历的创建接口；确认页仍须通过 `GET /approval-items/{approval_item_id}` 获取服务端保存的权威预览，以防 SSE 事件已过期或草稿状态发生变化。

**需要保存：** 日历草稿 ID、来源运行 ID、主题、起止时间、时区、参与者、日历、描述、版本；确认项目 ID、草稿预览快照、参数哈希、状态和过期时间。

### 步骤 3：进入确认页并加载“创建日历事件”单项

**页面操作：** 用户通过对话提示进入 `confirmation.html?run_id=run_01J...`。页面的可滚动区域加载该次任务的待确认项；日历事件与邮件发送作为两行独立项目展示。

| 页面动作 | 接口 | 请求/参数 | 页面使用的返回数据 |
|---|---|---|---|
| 加载待确认列表 | `GET /approval-items?run_id={run_id}&status=pending` | 查询参数 | 项目 ID、动作、风险、标题、简要预览、状态、到期时间 |
| 加载日历完整预览 | `GET /approval-items/{approval_item_id}` | 路径参数 | 会议主题、参与者、时间、时区、工作日历、核对提示 |

页面只允许用户勾选“创建日历事件”行；邮件项目保持独立，不能由本次勾选改变其状态。

**需要保存：** 确认页访问不新增业务记录；如需审计可记录查看时间。确认项仍以创建时的预览快照为准，不能在页面端临时改写。

### 步骤 4：勾选核对框并批准创建日历事件

**页面操作：** 用户核对主题、参与者、时间和工作日历，勾选“我已核对”，点击“批准并创建事件”。

**接口：** `POST /approval-items/{approval_item_id}/approve`

```json
{"checked":true}
```

服务端依次校验：当前用户归属、项目为 `pending`、未过期、勾选为真、草稿参数哈希未变化、日历连接仍有写入 scope。校验通过后，服务端签发并立即消费仅属于该项目的一次性确认令牌，再调用：

`POST /calendar/event-drafts/{draft_id}/execute`

执行接口仅在服务端内部携带 `approval_token` 与该确认项绑定的幂等键；浏览器不需要也不应取得令牌。

执行成功响应：

```json
{"data":{"approval_item_id":"apv_calendar_01J...","status":"executed","event_id":"provider_event_...","audit_event_id":"audit_01J..."}}
```

失败时返回 `failed` 或 `unknown` 及补救说明，不能显示“已创建”。

**需要保存：** 确认决策、决策时间、确认令牌使用时间、执行状态、幂等键、外部事件 ID、错误/结果与审计事件。邮件确认项目不发生任何状态变化。

### 3.2 汇总：风险与状态边界

| 阶段 | 风险 | 是否已产生外部副作用 |
|---|---|---|
| 可用时间查询 | `read` | 否 |
| 日历草稿与待执行计划 | `write`（待确认） | 否 |
| 用户批准、服务端执行 | `write` | 成功时才创建日历事件 |
| 用户拒绝或确认过期 | 无执行 | 否 |

---

## 3.3 邮件检索、摘要与待办提取

以下按 `research.html` 的页面操作设计。初始页面不传检索条件、不考虑分页，一次性展示当前登录用户已授权邮箱中的全部邮件元数据。Aegis 主服务保存邮件元数据快照和 LLM 派生结果；原始邮件仍以邮箱服务为准。浏览器只请求 Aegis 公开 API，邮件读取和 LLM 语义提取由服务端调用 Email Tool Server 完成，浏览器不直接调用邮箱服务。

### 步骤 1：打开页面，一次性加载当前用户全部邮件

**页面操作：** 用户打开 `research.html`。页面向 Aegis API 请求当前登录用户全部已授权邮件的列表；表格加载完成后显示客户/发件人、主题、接收时间与“尚未生成”的摘要状态。本期暂不提供关键词、时间范围或分页参数。

| 调用方 | 接口 | 请求数据 | 返回/用途 |
|---|---|---|---|
| 浏览器 → Aegis API | `GET /mail/messages` | 无；用户身份由登录态确定 | 当前用户全部邮件的元数据列表 |
| Aegis API → Email Tool Server | `mail.messages.list` | 当前用户有效邮箱连接 | 全部邮件的基础信息、外部邮件 ID 和来源版本 |
| Aegis API → Aegis 数据存储 | 邮件快照写入/更新 | 工具返回的邮件元数据 | 邮件列表快照，供页面展示与后续提取关联 |

浏览器请求：

```text
GET /api/v1/mail/messages
```

服务端从登录态取得 `tenant_id`、`user_id`，查询该用户有效且具有邮件读取 scope 的连接，再通过 Email Tool Server 获取邮件；随后以“用户 + 来源连接 + 外部邮件 ID”为范围更新邮件元数据快照并返回。用户 ID 不由浏览器传入。

返回示例：

```json
{"data":{"snapshot_at":"2026-09-24T01:00:00Z","items":[{"email_id":"email_01J...","thread_id":"thread_...","customer":"星云科技","from":"li@example.com","subject":"2026 年续约确认","received_at":"2026-09-23T08:00:00Z","source_version":"provider_change_42","extraction_status":"not_generated"},{"email_id":"email_02J...","thread_id":"thread_...","customer":"远航软件","from":"chen@example.com","subject":"下月服务安排","received_at":"2026-09-22T08:00:00Z","source_version":"provider_change_19","extraction_status":"completed"}]}}
```

未连接工作邮箱或缺少读取 scope 时，接口返回 `CONNECTION_REQUIRED`，其中包含 `provider`、`required_scopes` 和连接页面地址；页面提供连接入口，但不显示虚构邮件。

**需要保存的邮件快照：** 来源连接、外部邮件 ID/线程 ID、发件人、主题、接收时间、脱敏列表信息、来源版本（或更新时间）、快照同步时间和摘要生成状态。邮件正文只在用户随后点击“生成摘要”或“起草回复”时由服务器安全读取，不直接下发到列表页，也不作为默认长期快照保存。

### 步骤 1.1：切换邮件管理分区

`research.html` 以“收到 / 保存草稿 / 已发送”分区组织数据；分区切换只读取 Aegis 主服务已保存的业务数据，不直接调用 Email Tool Server。

| 分区 | 接口 | 返回的关键数据 |
|---|---|---|
| 收到 | `GET /mail/messages` | 邮件元数据快照、摘要生成状态 |
| 保存草稿 | `GET /mail/drafts` | 草稿 ID、主题、收件人、版本、更新时间、状态（`draft` / `pending_approval`） |
| 已发送 | `GET /mail/sent-messages` | 外部邮件 ID、主题、收件人、发送时间、发送状态、关联运行 ID |

用户在“保存草稿”分区可查看或编辑草稿，并点击“提交发送确认”；发送成功后，服务端将草稿状态更新为 `sent`，同时在“已发送”分区展示该邮件。

### 步骤 2：点击单行“生成摘要”，展示关键信息、待办和截止时间

**页面操作：** 用户在目标邮件行点击“生成摘要”。此操作是一次新的异步提取请求；页面将该行状态改为“正在生成”，并订阅该请求对应的 SSE。

| 调用方 | 接口 | 请求数据 | 返回/用途 |
|---|---|---|---|
| 浏览器 → Aegis API | `POST /mail/{email_id}/extraction-requests` | `refresh`（默认 `false`） | 已缓存提取结果，或新的 `run_id` 与 `events_url` |
| 服务端 Worker → Email Tool Server | `mail.messages.get` | 外部邮件 ID、授权连接 | 邮件正文/必要附件文本，仅供服务端提取；读取后计算内容哈希 |
| 服务端 Worker → LLM 服务 | `mail.extract_actions` | 已脱敏邮件内容、结构化提取指令 | 关键信息、待办、截止时间候选 |
| 浏览器 → Aegis API | `GET /runs/{run_id}/events` | SSE | 行状态、提取结果或失败信息 |

若该邮件已有“相同抽取版本 + 相同来源版本”的结果且 `refresh=false`，`POST /mail/{email_id}/extraction-requests` 直接返回该结果，不新建运行、不重复调用 LLM。来源版本改变，或用户指定 `refresh=true` 时，才重新读取正文并生成新的提取结果。否则返回：

```json
{"data":{"run_id":"run_extract_01J...","status":"running","events_url":"/api/v1/runs/run_extract_01J.../events"}}
```

完成事件：

```json
{"type":"mail_extraction_completed","data":{"email_id":"email_01J...","extraction_id":"mex_01J...","key_points":["客户要求在周五前确认续约价格"],"todos":[{"content":"确认续约报价","due_at":"2026-09-26T09:00:00Z","is_inferred":true}],"due_date_candidates":["2026-09-26T09:00:00Z"]}}
```

`is_inferred` 表示日期或待办是从邮件语义推断，页面必须提示用户核对；它不是长期记忆置信度，也不得写入长期记忆。

**本步骤的 LLM 边界：** 用户点击“生成摘要”时调用一次 `mail.extract_actions`。该次调用同时输出关键信息、待办候选和截止时间候选；待办候选尚不是已创建的待办。若未识别到明确待办，返回空 `todos`，页面显示“未识别到明确待办”，并保持“加入待办草稿”按钮禁用。

**需要保存的派生结果：** `extraction_id`、邮件 ID、关联邮件快照 ID、来源版本、内容哈希、抽取状态、模型/提示词版本、关键信息、待办、截止时间候选、生成时间和失败原因。原始正文仅在本次提取中暂存，生成完成后释放；再次打开页面根据来源版本和抽取版本决定是否复用摘要。

### 步骤 3：点击“加入待办草稿”

**页面操作：** 每封邮件的“待办与截止时间”单元格内固定展示“加入待办草稿”按钮。摘要未生成或没有待办候选时按钮禁用；生成待办和截止时间后，用户选择一个或多个待办并点击该按钮。这只在 Aegis 内部创建草稿，不写入外部任务服务。

**公开接口：** `POST /mail/{email_id}/todo-drafts`

```json
{"extraction_id":"mex_01J...","todo_indexes":[0]}
```

返回 `todo_draft_id`、草稿内容、截止时间和来源邮件。页面提示“已加入待办草稿”，用户之后可在待办功能中编辑或决定是否执行；一期不自动创建第三方任务。

**模型调用边界：** 此接口不调用 LLM。它只校验 `extraction_id` 属于当前用户和当前邮件，再将用户选中的、已由“生成摘要”产生的待办候选复制为站内待办草稿，避免重复产生模型成本或改写候选内容。

**需要保存：** 待办草稿 ID、内容、截止时间、来源邮件、来源提取结果、创建时间和草稿状态。

### 步骤 4：点击“起草回复”

**页面操作：** 每封邮件的“关键信息”单元格内固定展示“起草回复”按钮。用户点击该按钮即可起草回复；页面可附带补充要求，例如“语气简洁，询问报价确认时间”。该按钮不依赖待办是否已生成。

| 调用方 | 接口 | 请求数据 | 返回/用途 |
|---|---|---|---|
| 浏览器 → Aegis API | `POST /mail/{email_id}/reply-draft-requests` | `extraction_id`、可选 `instruction` | `run_id`、`events_url` |
| 服务端 Worker → LLM 服务 | `mail.compose_reply` | 相关邮件、提取结果、用户要求 | 收件人建议、主题、邮件正文 |
| 浏览器 → Aegis API | `GET /runs/{run_id}/events` | SSE | 草稿生成进度和 `draft_id` |

生成完成后 SSE 发送 `mail_reply_draft_completed`，包含 `draft_id`、收件人建议、主题与正文；服务端保存草稿。该流程会另行调用一次 `mail.compose_reply` LLM 能力，可使用原邮件、已有摘要/待办和用户补充要求，但不调用 Email Tool Server 的发送工具，也不会发送邮件。发送与逐项确认只在 3.4 中处理。

**需要保存：** 草稿 ID、来源邮件/提取结果、收件人和抄送建议、主题、正文、版本、创建时间和状态。

### 3.3 汇总：风险与状态边界

| 阶段 | 风险 | 是否产生外部副作用 |
|---|---|---|
| 加载邮件列表、读取邮件、LLM 提取 | `read` | 否 |
| 创建待办草稿、邮件回复草稿 | 站内草稿写入 | 不写入第三方服务 |
| 生成回复草稿 | `read` + 站内草稿写入 | 不发送邮件 |
| 后续发送邮件 | `sensitive` | 仅在 3.4 单独确认后执行 |

---

## 3.4 邮件起草与确认后发送

以下按用户页面操作示例的路径设计：用户先在 `research.html` 的“保存草稿”分区查看或编辑草稿，随后提交发送确认。`confirmation.html` 仅承担待执行动作的批准或拒绝，不展示、编辑或保存邮件草稿正文。该项目与“创建日历事件”等其他项目互不影响。

### 步骤 1：生成、编辑并冻结邮件草稿

**前置条件：** 3.3 的“起草回复”已生成站内邮件草稿，或 3.1 的 Agent 已根据任务生成草稿。草稿尚未发送，状态为 `draft`。

| 页面动作 | 接口 | 请求数据 | 返回数据 |
|---|---|---|---|
| 保存草稿修改 | `PATCH /mail/drafts/{draft_id}` | `to[]`、`cc[]`、`subject`、`body` | 最新草稿、`draft_version` |
| 生成发送确认项 | `POST /mail/drafts/{draft_id}/approval-items` | 可选 `source_run_id` | `approval_item_id`、冻结预览、过期时间 |

编辑草稿表单：

```json
{"to":["dev-team@example.com"],"cc":[],"subject":"项目同步会议跟进","body":"各位好，以下是今天会议的结论……"}
```

草稿编辑完成后，服务端将当前版本冻结为发送预览，并创建动作 `mail.send`、风险 `sensitive` 的独立确认项目。之后即使用户再次修改草稿，原确认项也必须失效，需重新生成预览。

**需要保存：** 草稿 ID、来源邮件/任务、收件人、抄送、主题、正文、发送账户、草稿版本、创建/更新时间；确认项目 ID、冻结草稿快照、参数哈希、状态和过期时间。

### 步骤 2：从保存草稿提交发送确认，并在确认页加载单项

**页面操作：** 用户在邮件管理的“保存草稿”分区点击“提交发送确认”，页面跳转 `confirmation.html?run_id={run_id}`。确认页只在内部滚动区域逐条展示待确认动作；用户定位“发送确认邮件”项，同一任务中的日历创建项作为另一条独立记录展示。

| 页面动作 | 接口 | 请求/参数 | 页面使用的返回数据 |
|---|---|---|---|
| 加载待确认列表 | `GET /approval-items?run_id={run_id}&status=pending` | 查询参数 | 项目 ID、动作、风险、标题、简要预览、状态和到期时间 |
| 加载发送确认项 | `GET /approval-items/{approval_item_id}` | 路径参数 | 动作、风险、关联 `draft_id` / 草稿版本、确认状态、核对提示和到期时间 |

确认项仅引用服务端保存的草稿版本，不携带或展示完整正文。用户如需查看或修改草稿，应返回邮件管理的“保存草稿”分区；保存修改后旧确认项失效，需重新提交确认。确认项响应示例：

```json
{"data":{"approval_item_id":"apv_mail_01J...","action":"mail.send","risk_level":"sensitive","draft_id":"draft_01J...","draft_version":3,"status":"pending","expires_at":"2026-09-25T06:30:00Z"}}
```

### 步骤 3：勾选核对框并批准发送

**页面操作：** 用户已在邮件管理的“保存草稿”分区核对收件人、抄送、主题、正文和发送账户；在确认页勾选“我已核对”，点击“批准并发送邮件”。

**公开接口：** `POST /approval-items/{approval_item_id}/approve`

```json
{"checked":true}
```

服务端依次校验当前用户归属、项目状态为 `pending`、未过期、勾选为真、冻结快照的参数哈希未变化、邮件连接仍具备发送 scope。校验通过后，服务端签发并立即消费仅属于该确认项目的一次性令牌，再由服务端调用 Email Tool Server：

`mail.messages.send`

浏览器不直接调用发送工具，也不获取确认令牌。Email Tool Server 使用已授权的工作邮箱发送邮件；发送工具必须使用与确认项目绑定的幂等键。

成功返回：

```json
{"data":{"approval_item_id":"apv_mail_01J...","status":"executed","provider_message_id":"provider_msg_...","sent_at":"2026-09-25T07:01:12Z","audit_event_id":"audit_01J..."}}
```

若邮件服务超时或结果未知，返回 `failed` 或 `unknown`，不能显示“已发送”。

**需要保存：** 用户确认决策、确认时间、令牌使用时间、发送状态、幂等键、外部邮件 ID、发送时间、错误/结果和审计事件。发送成功时，服务端将对应草稿状态更新为 `sent`，并写入一条可在“已发送”分区查询的已发送邮件记录。

### 步骤 4：拒绝单项发送

**页面操作：** 用户点击“拒绝此项”。

**接口：** `POST /approval-items/{approval_item_id}/reject`

```json
{"reason":"收件人需要调整"}
```

接口将该邮件项目更新为 `rejected` 并记录审计；不调用 Email Tool Server。日历事件或其他邮件项目保持原状态，用户仍可分别批准、拒绝或重新编辑对应草稿。

### 3.4 汇总：风险与状态边界

| 阶段 | 风险 | 是否产生外部副作用 |
|---|---|---|
| 起草、编辑、冻结邮件草稿 | 站内草稿写入 | 否 |
| 确认页查看邮件预览 | `read` | 否 |
| 批准并由服务端发送 | `sensitive` | 成功时才发送邮件 |
| 拒绝、过期或失败 | 无执行 | 否 |

---

## 3.7 长期记忆与个性化建议

### 页面输入与接口

一期只从用户手动保存或明确确认的偏好中取值。服务端在执行任务时内部检索；页面只获取本次任务实际使用的非敏感摘要。

| 页面动作 | 接口 | 表单/请求数据 | 返回数据 |
|---|---|---|---|
| 查看已使用偏好 | `GET /runs/{run_id}/context-summary` | 无 | 非敏感偏好摘要、使用目的、敏感内容过滤说明 |
| 查看个性化建议 | `GET /runs/{run_id}/recommendations` | 无 | 候选建议、引用偏好摘要、建议原因 |

```json
{"data":{"recommendations":["周二 15:00","周三 10:30"],"used_context":[{"summary":"周一上午不安排会议","source":"user_input"}],"sensitive_context_excluded":true}}
```

### 需要保存的数据与控制

- 偏好文本、来源（`user_input`/`user_confirmed`）、敏感标识、创建/更新时间、软删除标记。
- 任务上下文使用记录：任务 ID、记忆 ID、使用目的、是否因敏感性排除。
- 检索先过滤租户、用户、权限、删除状态和敏感等级，再按关键词或确定性规则匹配。
- 一期没有 AI 自动写入、会话自动总结、实体、向量、语义检索或记忆置信度字段。

---

## 3.8 记忆查看、编辑与删除

### 页面输入与接口

`memory.html` 仅用于用户管理自己的偏好记忆。

| 页面动作 | 接口 | 表单/请求数据 | 返回数据 |
|---|---|---|---|
| 搜索记忆 | `GET /memories?q=...&cursor=...` | 关键词、分页参数 | 记忆 ID、文本、来源、敏感性、更新时间 |
| 新增偏好 | `POST /memories` | `content`、`is_sensitive` | 新记忆，来源固定为 `user_input` |
| 确认写入偏好 | `POST /memories/confirm` | `content`、`is_sensitive`、`source_run_id` | 新记忆，来源为 `user_confirmed` |
| 编辑偏好 | `PATCH /memories/{memory_id}` | `content`、`is_sensitive` | 更新后详情 |
| 发起删除确认 | `POST /memories/{memory_id}/deletion-requests` | 无 | `deletion_token`、过期时间 |
| 确认删除 | `POST /memories/{memory_id}/delete` | `deletion_token` | 删除状态 |

### 需要保存的数据与控制

- 保存记忆 ID、偏好文本、来源、敏感标识、创建/更新时间、删除时间、删除确认令牌及审计事件。
- 用户只能管理自己的记忆；软删除后不能检索，灾备恢复也不得重新暴露。
- 列表和详情不返回 AI 置信度、AI 摘要、实体或向量字段。

---

## 4. 一期实现验收清单

1. 3.1 SSE 可展示消息、工具预览、进度和结束状态，并支持断线续传。
2. 3.2、3.4 的外部副作用都有独立确认项目，未批准不得执行。
3. 3.3 只在用户点击“生成摘要”时调用 LLM；同版本结果可复用，起草回复不发送邮件。
4. 3.7、3.8 只有用户输入或确认的偏好可进入长期记忆；一期不出现记忆置信度。
