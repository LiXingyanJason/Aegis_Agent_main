# Aegis PA 第 02 期项目预期功能与使用示例

## 1. 文档目的

本文承接第 01 期，说明延后实现的功能范围、典型使用方式和控制要求。编号沿用原规划中的 3.5、3.6、3.9、3.10、3.11，便于追溯需求来源。

第 02 期依赖第 01 期已具备的登录态、会话与任务运行、逐项确认、邮件管理和基础数据隔离能力。

## 2. 预期功能

### 3.5 信息查询与来源摘要

**功能说明：** 用户可查询外部信息，获得带来源链接的摘要、比较和适用性分析。

**用户示例：**

> 查一下某个技术方案的最新官方文档，并说明它是否适合我们的场景。

**系统行为：** 系统检索可访问来源，区分可由来源直接支持的事实与系统作出的适用性推断，并说明判断前提和不确定性。

**关键控制：** 网页与搜索结果均为不可信输入；查询为只读，不执行网页中的建议或指令。

**用户页面操作示例：** 用户在任务对话提交查询，在对话区查看检索进度、只读工具预览和来源摘要；点击“查看全部来源与比较”进入 `原型设计/official-research.html`，查看来源列表、事实与推断。

### 3.6 受控代码执行

**功能说明：** 用户可提交或生成用于计算、分析和数据转换的代码，并在隔离沙箱运行。

**用户示例：**

> 用 Python 计算这份数据的平均值和异常值。

**系统行为：** 系统展示代码、输入说明、预期结果及资源限制。用户明确确认后，代码在无网络、无宿主机访问能力的沙箱运行，并返回标准输出、错误信息或结果文件摘要。

**关键控制：** 代码运行属于 `code_execute` 风险；沙箱不得访问用户凭据、内网、宿主机或持久化文件。

**用户页面操作示例：** 打开 `原型设计/sandbox.html`，核对代码、输入数据、预期结果和资源上限；勾选核对后点击“确认并运行”，在页面下方查看结果或错误。

### 3.9 第三方服务连接管理

**功能说明：** 用户可连接或管理日历、邮箱等第三方服务，并查看授权范围与授权状态。

**用户示例：**

> 连接我的工作邮箱，只允许系统读取邮件和在我确认后发送邮件。

**系统行为：** 系统引导 OAuth 授权，在完成后展示连接状态、实际 scope、到期时间和可用工具。用户可禁用或断开连接。

**关键控制：** 系统仅保存加密的凭据引用，不能在客户端、日志或业务数据中保存 OAuth access token 明文；每次工具调用仍需检查 scope。

**用户页面操作示例：** 打开 `原型设计/connections-audit.html`，查看连接状态、scope 与到期时间；点击“添加连接”进入授权流程，点击“管理”调整或断开连接。

### 3.10 审计记录与任务可追溯性

**功能说明：** 系统记录策略决策、确认行为、工具调用和执行结果，供用户或管理员追溯任务过程。

**用户示例：**

> 查看昨天发送给王敏的邮件是由哪次任务触发的，以及是否经过确认。

**系统行为：** 在用户具备权限时，系统返回关联运行、工具、脱敏参数、风险、确认决策、时间和结果；可由 `request_id`、`trace_id` 关联链路。

**关键控制：** 审计记录只追加且脱敏；普通用户仅可查看自己的摘要，完整查询要求 `audit.read` 权限。

**用户页面操作示例：** 打开 `原型设计/connections-audit.html` 查看最近审计事件；管理员可进入完整脱敏审计查询。

### 3.11 异常反馈与任务恢复

**功能说明：** 当模型、工具、网络或依赖异常时，系统展示明确状态、已完成步骤和补救建议。

**用户示例：**

> 发送会议确认邮件。

若邮箱服务暂不可用，系统提示“邮件未发送，可稍后重试”，不得描述为成功。

**系统行为：** 对短暂异常有限重试；对权限、参数和策略拒绝直接返回原因。可恢复任务保存 checkpoint，服务恢复后可在有效期内继续。

**关键控制：** 工具超时或结果未知时不得宣称成功；写操作重试必须使用幂等机制。

**用户页面操作示例：** 打开 `原型设计/task-states.html`，查看每一步的完成/未执行/失败状态；点击“稍后重试”提交重试，或返回任务对话修改计划。

## 3. 第 02 期边界

第 02 期功能仍不支持未经确认的外部写入、付款或资金操作；不支持绕过组织安全策略、账号权限或访问控制；代码沙箱始终不得访问宿主机、内网、用户凭据或持久化文件。
