# Aegis PA 系统概要设计说明书

**项目名称：** Aegis PA（个人效率 Agent）  
**文档版本：** V1.0  
**编制日期：** 2026-09-23

## 变更历史

| 版本 | 日期 | 变更说明 |
|---|---|---|
| V1.0 | 2026-09-23 | 基于项目现有设计文档编制系统概要设计说明书 |

## 目录

1. 文档介绍
2. 任务概述
3. 总体设计
4. 客户端交互与接口设计
5. 运行部署
6. 出错处理设计
7. 安全保密设计
8. 维护设计

# 1. 文档介绍

## 1.1 文档目的

本文档用于明确 Aegis PA 的系统结构、模块职责、主要处理流程、部署方式及运行保障要求，为项目计划、详细设计、开发、测试、上线和运维提供共同依据。

Aegis PA 是隐私优先的个人效率助理。系统在用户授权范围内支持日程管理、邮件处理、信息查询和受控代码执行，并通过长期记忆提供个性化服务。系统不是无边界自动化工具；任何会产生外部副作用的操作均受策略控制，并需要用户确认。

## 1.2 文档范围

本文覆盖项目概述、总体架构、功能设计、技术架构、客户端交互、服务接口、运行部署、异常处理、安全保密和维护设计。

以下内容不在本文展开：

- HTTP 接口的字段、状态码和示例，详见《02-api-spec.md》。
- 数据表、索引和数据生命周期，详见《03-database-design.md》。
- 安全控制矩阵、告警阈值和验收标准，详见《04-security-operations.md》。

## 1.3 读者对象

本文的主要读者包括项目经理、产品人员、架构师、开发人员、测试人员、运维人员和安全审计人员。

## 1.4 参考文档

| 文档名称 | 用途 |
|---|---|
| 《README.md》 | 项目定位、文档导航和关键约束 |
| 《01-system-overview.md》 | 系统架构与核心模块说明 |
| 《02-api-spec.md》 | Agent、会话、记忆、工具和观测接口定义 |
| 《03-database-design.md》 | 数据存储、实体关系和记忆生命周期设计 |
| 《04-security-operations.md》 | 安全、运维、监控、告警和验收要求 |

## 1.5 术语与缩写解释

| 术语或缩写 | 说明 |
|---|---|
| Agent | 可理解用户意图、生成计划并在受控范围内调用工具的智能助理 |
| MCP | Model Context Protocol，用于统一接入外部工具和服务的协议 |
| LLM | 大语言模型，用于理解、规划、推理和内容生成 |
| RBAC | 基于角色的访问控制 |
| OAuth Scope | 第三方授权令牌所允许访问的最小权限集合 |
| LangGraph | 用于编排 Agent 状态机和可恢复工作流的框架 |
| Guardrails | 对输入、规划和输出进行安全约束与校验的机制 |
| SSE | Server-Sent Events，用于向客户端持续推送任务进度和回复文本 |

# 2. 任务概述

## 2.1 建设目标

建设生产级个人效率 Agent，在用户显式授权的范围内完成日历查询与管理、邮件检索与起草、信息查询、受控代码执行等任务。系统应在提升个人工作效率的同时，保证操作可确认、数据可隔离、执行可追溯、故障可恢复。

具体目标如下：

- 支持自然语言对话和流式展示任务进度。
- 支持日程、邮件、搜索和代码沙箱等工具的受控接入。
- 对写入、敏感读取和代码执行等风险操作强制执行“预览 → 确认 → 执行”。
- 支持长期记忆的存储、检索、更正和删除，且不发生跨用户、跨租户数据泄露。
- 支持全链路审计、运行观测、幂等执行和工作流断点恢复。

## 2.2 运行环境

### 2.2.1 服务端环境

| 类别 | 运行要求 |
|---|---|
| 操作系统 | Linux 容器运行环境，生产环境使用 Kubernetes |
| 应用服务 | FastAPI API Gateway、Agent Orchestrator、异步 Worker |
| 缓存与状态 | Redis，用于会话、限流、幂等和工作流 checkpoint |
| 数据库 | PostgreSQL，并启用 pgvector 支持语义检索 |
| 工具服务 | 独立部署的 MCP Server，包括 Calendar、Email、Search 和 Sandbox |
| 可观测性 | LangSmith、Prometheus、Grafana 和 Alertmanager |
| 密钥管理 | KMS 或 Secret Manager，保存凭据引用和加密材料 |

### 2.2.2 客户端环境

客户端可为 Web 应用、桌面应用或移动端。客户端通过 HTTPS 调用 `/api/v1` 接口，并通过 SSE 接收 Agent 的状态、预览、确认请求和最终结果。客户端不得保存第三方 OAuth token 或直接调用内部 MCP 服务。

## 2.3 需求概述

系统围绕个人日常工作流提供以下能力：

| 能力域 | 主要能力 | 风险等级 |
|---|---|---|
| 对话协作 | 理解用户请求、规划任务、输出结果和进度 | read |
| 日程管理 | 读取、创建和修改日历事件 | read / write |
| 邮件处理 | 检索、摘要、起草和确认后发送邮件 | read / sensitive |
| 信息查询 | 搜索外部信息并给出来源摘要 | read |
| 代码执行 | 在隔离沙箱中运行用户代码并返回结果 | code_execute |
| 长期记忆 | 保存偏好、事实与摘要，支持检索、更正和删除 | read / write |

## 2.4 条件与限制

### 条件

- 用户已通过系统认证，并对所使用的第三方服务完成 OAuth 授权。
- 所有可执行工具均已在工具注册表中定义输入 Schema、所需权限、风险等级和超时策略。
- Redis、PostgreSQL、密钥管理服务和外部 MCP Server 可用。

### 限制

- LLM 不具备最终权限判断权，不能绕过策略服务或确认网关。
- 邮件发送、日历写入和代码执行必须由用户确认；敏感操作可要求二次认证。
- 沙箱不得访问宿主机、内网、用户凭据或持久化文件，且默认无网络。
- 外部网页、邮件正文和检索内容均属于不可信输入，不能直接改变工具权限或执行计划。
- Redis 仅保存短期状态；长期记忆、实体资料和审计事件必须保存至 PostgreSQL。

# 3. 总体设计

## 3.1 体系结构设计

系统采用分层、可扩展的 Agent 服务架构。API Gateway 负责统一入口和基础安全控制；LangGraph Orchestrator 负责任务状态机编排；策略服务对所有工具调用进行确定性校验；MCP Tool Gateway 对接外部能力；Redis 和 PostgreSQL 分别承载短期状态与长期数据。

```text
Client
  │ HTTPS / SSE
  ▼
FastAPI API Gateway ── Auth / RBAC / Rate Limit
  │
  ├── LangGraph Orchestrator
  │     ├── Input and Output Guardrails
  │     ├── Intent and Risk Router
  │     ├── Model Router
  │     ├── Memory Retriever
  │     ├── Tool Planner
  │     └── Confirmation Gate
  │
  ├── Policy Service ── Scope / Allowlist / Confirmation Token
  ├── MCP Tool Gateway ── Calendar / Email / Search / Sandbox
  ├── Redis ── Session / Checkpoint / Idempotency / Rate Limit
  ├── PostgreSQL + pgvector ── Memory / Entity / Run / Audit
  └── LangSmith + Prometheus ── Trace / Metrics / Alert
```

系统中模型、策略和执行职责必须分离：LLM 提供意图判断、计划和文本生成；策略服务负责权限和风险判断；工具网关执行经过授权且确认完成的外部调用。

## 3.2 功能设计

### 3.2.1 Agent 对话与任务编排

Agent 对话模块接收用户消息，生成 `request_id`、`conversation_id` 和 `run_id`，并按以下状态机处理请求：

```text
RECEIVED → INPUT_GUARD → INTENT_ROUTE → MEMORY_RETRIEVE → PLAN
→ POLICY_CHECK → [CONFIRM] → TOOL_EXECUTE → VERIFY
→ MEMORY_WRITE → OUTPUT_GUARD → RESPONDED
```

风险等级为 `write`、`sensitive` 或 `code_execute` 时，流程必须进入 `CONFIRM` 状态。确认前仅可向用户展示脱敏后的工具名称、参数预览、风险说明和预期影响，不得执行实际副作用。

### 3.2.2 模型与意图路由

路由器输出意图、复杂度、风险建议、候选工具和置信度。低复杂度、无工具或只读请求优先使用小模型；多步骤规划、跨工具协作、长文生成和低置信度请求使用大模型。每次模型选择必须记录模型名称、token、时延、估算成本和路由原因。

### 3.2.3 工具调用与确认控制

工具调用遵循“发现 → Schema 校验 → 授权 → 确认 → 执行 → 验证 → 审计”的固定流程。

- 工具注册表维护工具名称、版本、输入 JSON Schema、所需 scope、风险等级、超时和重试策略。
- 策略服务在每次调用前校验 RBAC、OAuth scope、工具 allowlist 和资源归属。
- 写操作使用幂等键，避免网络重试或重复提交导致重复写入。
- 确认令牌为一次性签名令牌，默认有效期 10 分钟，并与已展示参数的哈希绑定；参数变化后必须重新确认。

### 3.2.4 记忆管理

短期记忆保存在 Redis，长期记忆保存在 PostgreSQL 与 pgvector。长期记忆包括会话摘要、用户偏好、事实和情境事件；结构化实体用于保存联系人、地点、时区和常用偏好等信息。

记忆检索必须先按租户、用户、删除状态、权限标签和敏感级别过滤，再执行向量近邻检索，并按相似度、重要性、时效性和置信度重排。默认不将敏感记忆直接注入模型提示词。用户可查看、更新和删除自己的长期记忆。

### 3.2.5 异步任务与审计

Worker 负责会话摘要、embedding 生成、记忆衰减、失败重试、向量清理和其他耗时后台任务。`agent_runs`、`tool_calls` 和 `audit_events` 记录请求、工具和策略决策；审计记录只追加，且只写入脱敏后的元数据。

## 3.3 技术架构

| 技术层 | 选型或机制 | 主要作用 |
|---|---|---|
| 接入层 | FastAPI、HTTPS、SSE | 提供 API、流式进度和统一入口控制 |
| 编排层 | LangGraph | 管理状态机、断点、恢复和节点级 trace |
| 智能层 | 小模型与大模型路由 | 分类、抽取、摘要、规划、推理和写作 |
| 安全层 | NeMo Guardrails、Policy Service | 输入/计划/输出防护、授权和确认控制 |
| 工具层 | MCP Tool Gateway | 工具发现、参数校验、执行适配和标准错误 |
| 数据层 | Redis、PostgreSQL、pgvector | 会话状态、业务数据、记忆和语义检索 |
| 可观测层 | LangSmith、Prometheus、Grafana | Trace、指标、仪表盘和告警 |

# 4. 客户端交互与接口设计

## 4.1 客户端交互设计

本项目不预设某一套固定页面原型，客户端可按业务形态实现 Web、桌面或移动端界面。但客户端交互必须体现以下状态和约束：

| 交互状态 | 客户端展示要求 |
|---|---|
| 任务处理中 | 展示当前工作节点或可理解的进度说明，并支持 SSE 断线续传 |
| 工具预览 | 展示工具名称、已脱敏参数、风险等级、预期影响和工具版本 |
| 需要确认 | 提供明确的批准与拒绝操作；不以默认勾选或隐含操作代替确认 |
| 执行完成 | 展示最终结果、相关来源或操作摘要，并关联请求标识 |
| 执行失败 | 展示标准错误说明、已完成步骤和可操作的补救建议 |

客户端应提供会话管理、记忆查看与编辑、第三方服务授权状态和审计查询入口。管理与审计页面仅对具备相应权限的用户开放。

## 4.2 接口设计

系统对外接口以 `/api/v1` 为统一前缀，使用 Bearer Token 认证、JSON 数据格式和 ISO 8601 UTC 时间。所有写请求建议携带 `Idempotency-Key`，服务响应应携带 `X-Request-Id`。

主要接口分组如下：

| 模块 | 接口能力 |
|---|---|
| Agent 对话 | 提交消息、获取 SSE 事件、提交确认决策 |
| 会话与记忆 | 查询会话、检索记忆、更新记忆、删除记忆 |
| 工具与连接 | 查询当前可用工具、发起 Calendar 或 Email OAuth 授权 |
| 管理与观测 | 查询审计事件、提供集群内部 Prometheus 指标 |

接口的请求、响应、SSE 事件、错误码和授权要求以《02-api-spec.md》为准。对于上游工具错误，系统须返回统一错误体，至少包含错误码、用户可读信息、`request_id` 和是否可重试。

# 5. 运行部署

## 5.1 运行模块的组合

生产环境由以下运行模块组成：

| 模块 | 部署形态 | 说明 |
|---|---|---|
| API | 无状态 Deployment | 提供 HTTP/SSE、认证、请求接入和编排入口 |
| Worker | 独立 Deployment | 执行摘要、embedding、清理、重试等异步任务 |
| Migration | 部署前 Job | 执行数据库 schema 和向量索引迁移 |
| MCP Server | 独立 Deployment | 对接 Calendar、Email、Search 和 Sandbox 能力 |
| Redis | 托管服务或 StatefulSet | 保存会话、checkpoint、确认状态和限流数据 |
| PostgreSQL | 托管服务或 StatefulSet | 保存业务、记忆、审计和 pgvector 数据 |

API 与 Worker 独立扩缩容。MCP Server 以 NetworkPolicy 与核心服务隔离；沙箱服务只接受受控的工具网关调用。

## 5.2 运行控制

系统应持续运行，并通过 readiness/liveness probe、资源 requests/limits、PDB、HPA 和滚动发布保障可用性。无外部工具的请求目标为 P95 首字响应小于 2 秒；包含工具调用的任务应持续向客户端输出进度。

每个运行实例应具备唯一日志标识，并通过 `request_id` 和 `trace_id` 将 API、LangGraph、模型、工具调用和审计事件关联起来。服务重启后，应能够从 Redis checkpoint 恢复未完成且未过期的工作流。

## 5.3 部署流程

部署采用容器镜像和 Helm 管理：

1. 在 CI 中执行依赖安装、单元测试、镜像扫描、镜像签名和镜像构建。
2. 将 API、Worker、Migration 和 MCP Server 镜像推送至受控镜像仓库。
3. 在目标环境通过 Helm 注入 ConfigMap、Secret 引用、资源限制和网络策略。
4. 先执行 Migration Job；成功后滚动发布 API、Worker 和 MCP Server。
5. 根据 readiness、错误率、时延和业务探针验证发布结果；异常时执行版本回滚。

凭据不得打包进镜像或写入日志。配置由 ConfigMap 提供，密钥仅通过 External Secrets 或 KMS/Secret Manager 引用注入。

# 6. 出错处理设计

## 6.1 出错输出信息

系统采用统一错误结构，对客户端返回安全、明确且可操作的信息。错误响应包含 `code`、`message`、`request_id` 和 `retryable` 字段。客户端不得展示内部堆栈、凭据、原始模型提示词或未脱敏 PII。

常见错误包括参数错误、未认证、权限不足、资源不存在、幂等冲突、策略拒绝、限流、上游工具失败及模型或依赖不可用。具体状态码以《02-api-spec.md》为准。

## 6.2 出错处理对策

| 错误场景 | 处理对策 |
|---|---|
| 输入不合法或 Schema 校验失败 | 拒绝执行并返回字段级或可读提示，不进入工具调用 |
| 权限或策略拒绝 | 记录审计事件，返回拒绝原因，不允许模型自行降级绕过 |
| 确认令牌过期或参数变化 | 终止待确认动作，要求重新生成预览和确认 |
| 瞬态网络或工具超时 | 按工具策略进行有限指数退避重试，并使用幂等键防止重复写入 |
| 不可重试的上游失败 | 返回已完成步骤和补救建议，模型不得声称调用成功 |
| Redis、数据库或模型依赖异常 | 降级只读能力或拒绝高风险操作；不得跳过鉴权、审计和确认 |
| Worker 任务失败 | 记录失败原因，进入受控重试或死信处理，并保留可追踪状态 |

# 7. 安全保密设计

## 7.1 安全控制

- 在输入、计划和输出三个阶段执行 Guardrails；所有外部内容标注为不可信输入。
- 每次工具调用均执行 OAuth scope、RBAC、工具 allowlist 和资源归属校验。
- 读操作仅在已授权 scope 内直接执行；写入、敏感操作和代码执行必须经过显式确认。
- 确认令牌一次性使用，并绑定操作参数哈希、用户和有效期。
- 消息原文、长期记忆和敏感业务字段加密保存；日志、trace 和审计中仅保存脱敏数据。
- OAuth access token 不得明文保存在业务数据库，业务库仅保存 Secret Manager 或 KMS 的引用。
- 代码沙箱采用独立非特权容器、只读根文件系统、无网络、CPU/内存限制和执行时长限制。

## 7.2 安全审计与监控

审计事件记录操作者、动作、目标资源、策略决策、原因、运行标识和脱敏元数据，并按月分区、只追加保存。系统应监控请求量、请求时延、模型 token 与成本、模型路由、工具调用、Guardrail 拦截、确认结果和记忆检索时延。

出现 API 5xx 比例上升、P95 时延超标、工具失败率异常、Guardrail 拦截突增、模型成本异常或确认令牌验证失败激增时，应触发告警并进入运行处置流程。

# 8. 维护设计

维护设计以可观测、可追溯、可恢复和可演进为目标。

- **日志与追踪：** API、状态机节点、模型调用、工具调用和审计事件均以 `request_id`、`trace_id` 关联；日志中不得包含令牌、邮箱地址、用户原文和原始提示词。
- **数据维护：** 定期执行数据库备份和恢复演练；对过期、低价值记忆进行衰减、归档或删除；用户删除后不得在灾备恢复中重新暴露该记忆。
- **运行维护：** 通过仪表盘监控 SLO、资源使用、队列积压和依赖健康状态；定期演练外部工具、Redis、数据库和模型服务故障的降级与恢复。
- **安全维护：** 定期轮换密钥和 OAuth 授权，扫描镜像与依赖漏洞，更新工具 allowlist 和 Guardrails 规则，并开展注入与越权红队测试。
- **版本维护：** 工具、模型和 embedding 维度均需版本化；更换 embedding 模型时采用双写、回填和切换策略，避免新旧向量混用。

系统后续新增工具或记忆类型时，必须同步补充工具 Schema、最小 scope、风险等级、确认要求、审计动作、异常语义、监控指标和验收用例。
