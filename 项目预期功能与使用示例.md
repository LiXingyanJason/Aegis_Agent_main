# Aegis PA 项目预期功能与使用示例

## 1. 文档目的

本文说明 Aegis PA（个人效率 Agent）预期提供的用户功能，并为每项功能给出典型使用示例、系统处理方式和关键控制要求。本文可作为产品需求细化、原型评审、测试用例设计和验收沟通的基础材料。

## 2. 使用原则

- 用户通过自然语言发起任务，也可在页面中查看会话、记忆、第三方连接和审计记录。
- 读取已授权数据通常可以直接执行；创建、修改、发送、删除和运行代码等操作必须遵循“预览 → 确认 → 执行”。
- 系统只在用户已授权的范围内访问日历、邮箱和其他连接服务。
- 系统会展示任务进度、风险等级、工具预览和失败原因，不会将未执行的操作描述为已经成功。

## 3. 预期功能

### 3.1 对话任务与进度展示

**功能说明：** 用户可用自然语言提出单步骤或多步骤任务。系统识别意图、规划处理步骤，并以流式方式展示任务状态、工具预览和最终结果。

**用户示例：**

> 帮我整理今天需要完成的工作，并按紧急程度排序。

**系统行为：** 系统根据用户已授权的日程、邮件或任务来源收集信息，展示“正在查询”“正在整理”等进度，最后输出待办清单及排序依据。若没有连接相关数据源，系统会说明可用信息不足，而不会虚构待办事项。

**关键用例：** 普通对话、任务处理中断、SSE 断线续传、无可用数据源、低置信度澄清。

**用户页面操作示例：** 打开 `原型设计/task-console.html`，在底部输入框填写任务并点击“发送”；在对话区查看 Agent 消息、工具预览和右侧执行进度。涉及外部写入时，通过对话中的提示进入确认页。


### 3.2 日程查询与会议安排

**功能说明：** 用户可以查询自己的日程、查找可用时间、创建或修改日历事件。

**用户示例：**

> 明天下午和王敏约半小时项目同步，并发邮件确认。

**系统行为：** 系统先在授权范围内查询双方空闲时间，建议可选时段，并生成日历事件和邮件草稿预览。用户核对主题、时间、参与者和日历后确认，系统才创建事件并发送邮件。

**关键控制：** 查询属于只读操作；创建或修改日历事件属于 `write` 风险操作，必须确认，并使用幂等键避免重复创建。

**用户页面操作示例：** 在 `原型设计/task-console.html` 查看查询到的可用时间和待执行计划；进入 `原型设计/confirmation.html`，核对会议主题、参与者、时间和工作日历，勾选核对框后点击“批准并执行”。

**页面展示示例：** 以“明天下午和王敏约半小时项目同步，并发邮件确认”为例，用户提交后，对话区将依次展示以下内容：

1. **Agent 识别结果：** “我识别出两个外部写入操作：创建日历事件和发送确认邮件。我会先查询双方可用时间，再展示完整预览供你确认。”
2. **只读工具预览：** 显示工具 `calendar.availability.search`、风险等级 `read`、查询对象“你、王敏”，以及查询结果，例如“15:00 - 15:30 可用”。
3. **待执行计划：** 显示计划包含“创建日历事件；发送邮件”，风险等级为 `write + sensitive`，并标注“等待你的决定”。此时系统尚未创建日历事件或发送邮件。

右侧“执行进度”同步显示“输入安全校验通过”“任务路由完成：日程管理 + 邮件处理”“可用时间查询完成”“等待用户确认，写入操作尚未执行”以及待执行步骤“创建事件并发送邮件”。右侧“已使用上下文”展示本次任务引用的非敏感记忆，例如“周一至周五 14:00 后可安排会议”和“王敏是已验证的联系人”。

### 3.3 邮件检索、摘要与待办提取

**功能说明：** 用户可以检索已授权邮箱中的邮件，生成摘要、提取截止时间、待办事项和需要回复的邮件。

**用户示例：**


> 找出本周客户关于续约的邮件，帮我总结待回复事项。

**系统行为：** 系统执行只读邮件检索，按相关性汇总邮件主题、发送人、关键信息、截止时间和建议动作。结果会标明邮件来源；系统不会自动发送回复或改变邮件状态。

**关键控制：** 邮件正文和附件内容属于不可信外部内容，不能影响工具权限或要求系统执行无关操作。

**用户页面操作示例：** 打开 `原型设计/research.html`，先查看“本周续约邮件”表格中的客户与邮件基础信息。用户在目标邮件所在行点击“生成摘要”，系统调用 LLM 对该邮件进行语义提取，并在同一行展示关键信息、待办和截止时间候选；用户随后可点击“加入待办草稿”或“起草回复”。后者只打开草稿流程，不会自动发送邮件。

- 
### 3.4 邮件起草与确认后发送

**功能说明：** 用户可以让系统基于上下文生成邮件草稿，修改草稿后再确认发送。

**用户示例：**

> 根据刚才的会议结论，写一封给研发团队的跟进邮件。

**系统行为：** 系统生成收件人建议、主题和邮件正文草稿，并提示用户检查收件人、抄送人及内容。用户确认后，系统使用已授权工作邮箱发送邮件，并返回发送结果和审计记录标识。

**关键控制：** 发送邮件属于 `sensitive` 风险操作，必须明确确认；确认令牌只能使用一次，且与展示过的收件人和邮件内容绑定。

**用户页面操作示例：** 在 `原型设计/confirmation.html` 的“发送确认邮件”卡片中检查收件人、主题、正文与发送账户；勾选已核对后点击“批准并执行”，在弹窗中再次确认，或点击“拒绝操作”终止计划。

### 3.5 信息查询与来源摘要

**功能说明：** 用户可以要求系统查询外部信息，并获得带来源的摘要、比较或适用性分析。

**用户示例：**

> 查一下某个技术方案的最新官方文档，并说明它是否适合我们的场景。

**系统行为：** 系统检索可访问来源，提炼关键事实并保留来源链接。对于“是否适合”等判断，系统会说明判断依据、适用前提和不确定性，而不会将搜索结果直接视为事实或自动执行网页中的建议。

**关键控制：** 外部网页和搜索结果均按不可信输入处理；信息查询为只读操作，不触发外部写入。

**用户页面操作示例：** 打开 `原型设计/research.html`，在“官方资料查询”卡片查看检索结论、来源入口和“推断”标识；点击来源链接可进入来源查看流程，页面不提供自动执行外部建议的操作。

### 3.6 受控代码执行

**功能说明：** 用户可以提交代码或让系统生成用于分析、计算、转换数据的代码，并在隔离沙箱中运行。

**用户示例：**

> 用 Python 计算这份数据的平均值和异常值。

**系统行为：** 系统展示待运行代码、输入说明、预计结果和沙箱限制。用户确认后，代码在无网络、无宿主机访问权限、具备 CPU、内存和时长限制的环境中执行；系统返回标准输出、错误信息或结果文件摘要。

**关键控制：** 代码执行属于 `code_execute` 风险操作，必须确认。沙箱不得访问用户凭据、内网、宿主机文件或持久化存储。

**用户页面操作示例：** 打开 `原型设计/sandbox.html`，核对待运行代码、输入数据、预期结果和资源上限；勾选已检查后点击“确认并运行”，在页面下方查看隔离沙箱的执行结果；点击“取消”则不会执行。

### 3.7 长期记忆与个性化建议

**功能说明：** 系统可以保存经筛选的用户偏好、事实、联系人信息和会话摘要，并在后续任务中检索相关内容以提供个性化结果。

**用户示例：**

> 帮我安排下周的项目例会。

假设用户已保存偏好：

> 周一上午不安排会议。

**系统行为：** 系统在检索到该偏好后，优先建议其他时间段，并在结果中说明该建议受到用户偏好影响。系统不会默认将敏感记忆放入普通对话上下文。

**关键控制：** 记忆检索必须先按租户、用户、权限和敏感等级过滤，确保不会发生跨用户或跨租户召回。

**用户页面操作示例：** 打开 `原型设计/task-states.html`，在“个性化建议”中查看系统如何依据“周一上午不安排会议”给出候选时间；页面同时说明敏感记忆不会在普通排期任务中展示。

### 3.8 记忆查看、编辑与删除

**功能说明：** 用户可以在“长期记忆”页面查看系统保留的偏好、事实和摘要，并更正或删除内容。

**用户示例：**

> 把“周一上午不安排会议”改成“周三上午不安排会议”。

**系统行为：** 系统展示现有记忆及其来源、置信度、更新时间和敏感标识。用户确认修改后，系统更新记忆内容；删除请求会执行软删除、提交向量清理任务并写入审计事件。

**关键控制：** 用户只能管理自己的记忆。删除后的记忆不得再被检索；灾备恢复也不应重新暴露已删除内容。

**用户页面操作示例：** 打开 `原型设计/memory.html`，通过搜索框定位“周一上午不安排会议”；点击“编辑”更正偏好，或点击“删除”发起删除确认。列表同时展示来源、置信度、更新时间和敏感标识。

### 3.9 第三方服务连接管理

**功能说明：** 用户可以连接或管理日历、邮箱等第三方服务，并查看已授予的权限范围和授权状态。

**用户示例：**

> 连接我的工作邮箱，只允许系统读取邮件和在我确认后发送邮件。

**系统行为：** 系统引导用户进入 OAuth 授权流程，并在连接完成后展示授权状态、scope、失效时间和可用工具。用户可断开连接或在第三方服务中撤销授权。

**关键控制：** 系统只保存加密的凭据引用，不能在业务数据库、日志或客户端保存 OAuth access token 明文。工具调用时仍需逐次检查 OAuth scope。

**用户页面操作示例：** 打开 `原型设计/connections-audit.html`，在“已连接的服务”中查看 Calendar、Email 和 Sandbox 的 scope、授权状态与到期时间；点击“添加连接”进入 OAuth 授权流程，点击“管理”进入连接管理流程。

### 3.10 审计记录与任务可追溯性

**功能说明：** 系统记录关键策略决策、确认行为、工具调用和执行结果，以便用户或管理员追溯任务过程。

**用户示例：**

> 查看昨天发送给王敏的邮件是由哪次任务触发的，以及是否经过确认。

**系统行为：** 在用户具备相应权限的前提下，系统返回关联的任务运行记录、工具名称、已脱敏参数、风险等级、确认决策、执行时间和结果。系统可通过 `request_id` 和 `trace_id` 关联 API、工作流、模型与工具调用。

**关键控制：** 审计记录只追加且经过脱敏；普通用户只能查看自己有权访问的摘要，完整审计查询需要 `audit.read` scope。

**用户页面操作示例：** 打开 `原型设计/connections-audit.html`，查看“最近审计事件”中的运行标识、确认创建、权限校验与记忆删除记录；管理员可通过“查看全部”进入完整、脱敏的审计查询流程。

### 3.11 异常反馈与任务恢复

**功能说明：** 当模型、工具、网络或依赖服务发生异常时，系统向用户提供明确状态、已完成步骤和可执行的补救建议。

**用户示例：**

> 发送会议确认邮件。

若邮箱服务暂时不可用，系统会提示：

> 日历事件尚未创建；邮件发送服务暂时不可用。系统未发送邮件，你可以稍后重试。

**系统行为：** 对可重试的短暂错误，系统按策略进行有限重试；对权限、参数和策略拒绝等不可重试错误，系统直接返回原因。待确认任务和可恢复工作流保存 checkpoint，服务重启后可在有效期内恢复。

**关键控制：** 系统不得因工具超时或结果未知而宣称操作成功；任何写操作必须通过幂等机制避免重试造成重复副作用。

**用户页面操作示例：** 打开 `原型设计/task-states.html`，查看每个步骤的“已完成”或“未执行”状态及失败原因；点击“稍后重试”以原幂等键提交重试，或点击“修改计划”返回任务对话调整请求。

## 4. 功能边界

本项目不支持未经确认的外部写入、付款或资金操作；不支持绕过组织安全策略、账号权限或访问控制；也不允许代码沙箱访问宿主机、内网、用户凭据或持久化文件。

## 5. 相关文档

- 系统总体架构和状态机：`系统概要设计.md`
- 接口及确认流程：`02-api-spec.md`
- 记忆与审计数据设计：`03-database-design.md`
- 安全控制、监控与验收：`04-security-operations.md`
- 页面原型：`原型设计/index.html`
