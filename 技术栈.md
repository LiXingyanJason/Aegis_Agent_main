# Aegis PA 技术栈与分层方案

## 1. 目标与技术选型原则

本项目是一个具备任务对话、日程与邮件协作、受控外部写入和用户偏好的个人效率 Agent。技术栈以 **Python 后端、Vue 前端** 为硬性边界，采用“模块化单体 + 独立 Tool Server”作为第 01 期实现形态：先保证任务链路、确认机制和数据隔离可用，再按需要拆分服务。

第 01 期不引入服务注册中心、区块链、全文搜索集群或独立消息队列；这些组件不能直接解决当前业务问题，会增加部署与运维成本。第 02 期再加入信息查询、沙箱、连接管理、审计查询和异常恢复能力。

## 2. 总体架构

```text
Vue 3 Web（用户界面）
        │ HTTPS / SSE
        ▼
Nginx（静态资源、反向代理、TLS、限流）
        │
        ▼
Python FastAPI 应用
 ├─ API / Auth / RBAC / Idempotency
 ├─ Agent Orchestrator（路由、规划、状态机）
 ├─ Policy & Confirmation Gate（风险、预览、逐项确认）
 ├─ MCP Tool Gateway
 │    ├─ Calendar MCP Server
 │    └─ Email MCP Server
 ├─ Worker（LLM 摘要、邮件草稿、重试等异步任务）
 └─ Telemetry（Trace、Metrics、结构化日志）
        │                 │
        ▼                 ▼
PostgreSQL            Redis
业务数据、会话历史       会话短期上下文、SSE 游标、
任务运行、草稿、确认项    checkpoint、限流、幂等缓存
```

日历和邮件 Tool Server 是对第三方 API 的适配层，不保存 Aegis 的任务、草稿、LLM 摘要或审计数据；这些业务数据由 Aegis 主服务保存。

## 3. 技术栈清单

| 层级 | 推荐技术 | 在项目中的职责 |
|---|---|---|
| 前端框架 | Vue 3 + TypeScript | 构建任务对话、邮件管理、确认中心与记忆管理页面 |
| 前端构建 | Vite | 本地开发、构建与环境变量注入 |
| 前端路由 | Vue Router | 对话、邮件管理、确认、长期记忆等页面路由 |
| 前端状态 | Pinia | 当前会话、`run_id`、SSE 状态、确认项和页面级缓存 |
| UI 组件 | Element Plus | 表格、表单、抽屉、对话框、通知、加载和空状态 |
| HTTP / 流式通信 | Axios + EventSource | 普通 REST 请求与任务 SSE 事件订阅、断线续传 |
| 反向代理 | Nginx | TLS 终止、静态资源托管、反向代理、基础限流与请求大小限制 |
| 后端框架 | Python + FastAPI | REST API、SSE、鉴权依赖注入、接口文档与异步 I/O |
| 数据校验 | Pydantic | API、工具参数、LLM 结构化输出的 Schema 校验 |
| 业务持久化 | SQLAlchemy 2 + Alembic | 业务对象映射与数据库迁移 |
| Agent 编排 | LangGraph | 任务状态机、节点中断/恢复、工具规划与确认闸门 |
| 模型接入 | Python LLM Provider Adapter | 统一不同模型的调用、模型路由、成本与时延记录；不让业务代码直接绑定单一模型 SDK |
| MCP 集成 | MCP Python SDK + 自建 MCP Tool Server | 工具注册、发现、Schema 调用及 Calendar / Email 服务适配 |
| 关系数据 | PostgreSQL | 作为系统事实来源，保存会话、消息、运行、邮件快照、草稿、确认项、记忆与审计元数据 |
| 向量检索 | pgvector（PostgreSQL 扩展，第 02 期启用） | 在 PostgreSQL 中保存受治理记忆/知识的向量与索引，用于语义检索，避免一期引入独立向量数据库 |
| 短期状态 | Redis | 对话短期上下文、工作流 checkpoint、SSE 游标、确认令牌、幂等结果、限流 |
| 异步任务 | Celery + Redis | LLM 邮件摘要、回复草稿、同步和有限重试等耗时任务 |
| 输入/输出安全 | NeMo Guardrails + 自定义确定性策略 | 注入提示、输出约束；权限和确认仍由确定性策略服务决定 |
| PII 检测与脱敏 | Microsoft Presidio + 自定义规则 | 日志、审计和模型输入输出中的敏感字段检测/掩码 |
| 可观测性 | OpenTelemetry + Prometheus + Grafana | API、Agent、LLM、工具调用的 Trace、时延、成本与指标 |
| 日志与告警 | JSON 结构化日志 + Loki + Alertmanager | 故障排查、告警与运行趋势查看 |
| 测试 | Pytest、httpx、Vitest、Playwright | 后端单元/API、前端单元、关键用户流程端到端测试 |
| 部署 | Docker Compose（开发）+ Docker / Kubernetes（后续） | 本地一键启动、镜像交付和后续弹性部署 |

## 4. 前端方案

### 4.1 页面与状态划分

| Vue 页面/模块 | 主要 Pinia 状态 | 关键能力 |
|---|---|---|
| 任务对话 | `conversationStore`、`runStore` | 创建/恢复会话、发送任务、连接 SSE、展示进度与工具预览 |
| 邮件管理 | `mailStore` | “收到 / 保存草稿 / 已发送”分区、摘要、待办草稿、草稿编辑与提交发送确认 |
| 操作确认 | `approvalStore` | 按 `run_id` 加载独立确认项、批准或拒绝、展示真实执行状态 |
| 长期记忆 | `memoryStore` | 查看、创建、编辑、软删除用户手动维护的偏好 |

前端只保存当前页面需要的 `conversation_id`、`run_id`、`approval_item_id` 等引用；这些 ID 的创建、归属校验和状态更新均在后端完成。SSE 使用 `Last-Event-ID` 续传，不将任务执行状态仅保存在浏览器内存。

### 4.2 前端交互约束

- 写操作统一通过 `Idempotency-Key` 调用后端；浏览器不保存确认令牌。
- 邮件草稿在“邮件管理”页查看和编辑；“操作确认”页只批准或拒绝发送动作。
- 收到邮件列表只展示元数据快照；邮件正文只在生成摘要或起草回复时由后端按授权读取。
- 前端不直接调用 Calendar MCP Server、Email MCP Server 或第三方 OAuth API。

## 5. Python 后端方案

### 5.1 模块划分

```text
backend/
├─ app/api/             # FastAPI 路由、SSE、鉴权依赖
├─ app/domain/          # 会话、运行、邮件、草稿、确认、记忆等领域逻辑
├─ app/agent/           # LangGraph 状态机、智能路由、模型适配器
├─ app/policy/          # 风险分级、RBAC、scope、确认令牌与脱敏策略
├─ app/tools/           # MCP Client、工具注册表、Tool Gateway
├─ app/workers/         # Celery 任务与重试策略
├─ app/repositories/    # SQLAlchemy 数据访问
├─ app/observability/   # Trace、指标、结构化日志
└─ app/security/        # 输入检测、PII、密钥引用与审计辅助
```

### 5.2 智能路由

智能路由由 LangGraph 中的确定性节点和模型能力共同完成：

1. **意图识别**：识别日程、邮件、记忆或普通问答任务；输出结构化 `intent`。
2. **模型选择**：按任务复杂度、是否需要工具、上下文规模、预计成本和失败回退策略选择模型。
3. **工具选择**：根据工具注册表中的名称、风险等级、所需 scope 和 JSON Schema 选择工具。
4. **策略检查**：任何模型输出都必须再经过参数 Schema、权限和风险策略校验；模型不能自行扩大权限或绕过确认。

模型路由的“置信度”只用于决定是否澄清或升级模型，不等同于长期记忆置信度。第 01 期长期记忆不使用 AI 置信度评分。

### 5.3 MCP 工具调用系统

| 组件 | 职责 |
|---|---|
| MCP Tool Registry | 保存工具名、版本、输入 Schema、输出 Schema、风险等级、所需 scope、超时与状态 |
| MCP Tool Gateway | 执行参数校验、用户授权检查、确认项目校验、幂等控制、标准错误转换和 trace 透传 |
| Calendar MCP Server | 将 `calendar.availability.search`、`calendar.event.create` 等工具适配到日历服务 |
| Email MCP Server | 将 `mail.messages.list/get`、`mail.compose/send` 等工具适配到邮件服务 |

浏览器只调用 Aegis API。Agent 通过 Tool Gateway 调用 MCP Server；所有工具调用均关联 `run_id`、`tool_call_id` 和 `trace_id`。

## 6. 数据与记忆方案

### 6.1 第 01 期数据边界

| 数据类别 | 存储位置 | 保存内容 |
|---|---|---|
| 会话与任务 | PostgreSQL | `conversation_id`、消息、`run_id`、步骤、工具调用、确认项、草稿与结果 |
| 邮件快照 | PostgreSQL | 外部邮件 ID、线程 ID、主题、发件人、时间、来源版本、摘要状态；不默认保存正文 |
| LLM 派生结果 | PostgreSQL | 摘要、待办候选、截止时间候选、抽取版本、内容哈希 |
| 长期记忆 | PostgreSQL | 仅用户手动输入或明确确认的文本偏好、来源和敏感标识 |
| 短期上下文 | Redis | 当前会话窗口、状态机 checkpoint、SSE 游标、幂等结果与确认令牌 |

第 01 期不保存 AI 自动会话摘要、实体、向量或记忆置信度；也不把 PostgreSQL 当作邮箱全文镜像。

### 6.2 第 02 期记忆演进

在具备用户纠错、版本治理、删除传播和反馈闭环后，可启用：

- PostgreSQL + pgvector：存储经治理的 embedding，用于语义检索；
- 会话自动摘要、实体提取、重要性与衰减策略；
- 检索结果的来源、模型版本与可撤销性记录。

向量检索必须先经过租户、用户、权限、删除状态和敏感等级过滤；敏感记忆默认不得直接注入模型提示词。

## 7. 安全防护方案

| 防护点 | 技术与实现要求 |
|---|---|
| 输入检测 | NeMo Guardrails 规则、外部内容不可信标记、长度/格式限制；邮件和网页内容不得充当工具指令 |
| 输出过滤 | Presidio 与规则掩码 PII；输出前过滤密钥、令牌、未授权邮箱内容和危险建议 |
| 身份与权限 | OIDC/OAuth 2.0 登录、后端 RBAC、每次工具调用校验 provider scope |
| 外部写入 | 风险分级、参数预览、独立确认项、一次性确认令牌、参数哈希和幂等键 |
| 密钥管理 | OAuth token 仅存密钥管理系统或加密凭据引用；不出现在浏览器、业务日志或审计详情 |
| 数据隔离 | 所有业务读取按 `tenant_id + user_id` 过滤；必要时启用数据库 RLS |
| 审计 | 记录脱敏后的动作、决策、工具结果和关联标识；完整审计查询放入第 02 期 |

安全策略必须是可测试的确定性后端逻辑，不能仅依赖 LLM 或 Guardrails 的自然语言判断。

## 8. 可观测性方案

每次请求产生 `request_id`；每个任务运行产生 `run_id`；跨 API、Agent、模型和工具调用传播 `trace_id`。

| 维度 | 指标/数据 |
|---|---|
| 链路 | API → LangGraph 节点 → LLM → MCP Tool → 外部服务的 Trace |
| 性能 | 首字响应、任务总耗时、工具时延、SSE 断线/恢复次数 |
| 成本 | 模型、输入/输出 token、模型路由、预计成本 |
| 可靠性 | 工具失败率、重试次数、确认过期率、幂等冲突、结果未知数 |
| 安全 | Guardrail 拦截数、权限拒绝数、PII 掩码数、确认失败数 |

OpenTelemetry 负责生成和传播 Trace、Metrics、Logs；Prometheus 采集指标，Grafana 展示仪表盘，Alertmanager 配置错误率、时延、工具失败和成本异常告警。A/B 测试能力放入第 02 期，在模型/提示词版本和任务结果的可追溯基础上实施。

## 9. 与参考架构图的取舍

| 参考图元素 | 本项目采用方式 | 原因 |
|---|---|---|
| Vue.js | Vue 3 + TypeScript | 满足前端技术要求并便于组件化 |
| Element UI | Element Plus | Vue 3 对应组件库 |
| Vuex | Pinia | 适合 Vue 3 的状态管理方案 |
| Webpack | Vite | 用于更轻量的现代前端构建 |
| Nginx | 保留 | 提供反向代理、TLS 与静态资源服务 |
| Gateway + Nacos | 一期不引入 | 当前采用模块化单体，不需要服务注册与动态 DNS |
| MySQL | PostgreSQL | 更适合本项目的结构化业务数据与二期 pgvector 扩展 |
| Redis | 保留 | 承载短期状态、checkpoint、限流与幂等缓存 |
| Elasticsearch | 一期不引入 | 当前邮件以元数据快照和数据库查询满足需求；后续按检索规模评估 |
| RocketMQ | 一期使用 Celery + Redis | Python 生态实现异步任务更直接；高吞吐需求出现后再评估独立消息队列 |
| Docker | 保留 | 统一开发、测试和部署环境 |
| 区块链 | 不采用 | 与本项目的 Agent、邮件和日程业务无直接收益 |

## 10. 实施顺序

1. 搭建 Vue 3、FastAPI、PostgreSQL、Redis、Docker Compose 的基础工程；完成登录态、会话、SSE 和统一错误响应。
2. 实现 Agent 路由、LangGraph 状态机、MCP Tool Gateway，以及 Calendar / Email Tool Server 的只读工具。
3. 实现邮件管理、草稿、逐项确认、日程创建与邮件发送的幂等链路。
4. 接入 PII 脱敏、OpenTelemetry、Prometheus 与基础仪表盘；补齐 Pytest、Vitest、Playwright 测试。
5. 第 02 期再实施官方信息查询、代码沙箱、连接中心、审计查询、异常恢复及 pgvector 记忆增强。

## 11. 参考资料

- [FastAPI 官方文档](https://fastapi.tiangolo.com/)
- [Vue 官方快速开始](https://vuejs.org/guide/quick-start.html)
- [Vue Router 官方文档](https://router.vuejs.org/guide/)
- [Pinia 官方文档](https://pinia.vuejs.org/introduction.html)
- [OpenTelemetry Python 文档](https://opentelemetry.io/docs/languages/python/)
- [Model Context Protocol 文档](https://modelcontextprotocol.io/docs/learn/architecture)

## 12. 技术说明速览

以下说明面向第 01 期开发，侧重各技术在 Aegis PA 中实际承担的工作。

### 12.1 前端与接入层

| 技术 | 简要说明 |
|---|---|
| Vue 3 | 前端界面框架，用组件组织任务对话、邮件、确认中心等页面。 |
| TypeScript | 为 JavaScript 增加类型约束，在开发时提前发现接口字段或状态使用错误。 |
| Vite | 前端开发服务器和打包工具，负责本地热更新、构建生产静态文件及注入环境变量。 |
| Vue Router | 管理浏览器 URL 与前端页面的对应关系，例如从对话页切换到邮件管理页。 |
| Pinia | 集中保存前端运行状态，如当前会话、任务运行状态、确认项和页面缓存。 |
| Element Plus | Vue 3 的现成 UI 组件库，用于表格、表单、弹窗、通知等通用界面元素。 |
| Axios | 浏览器发起普通 HTTP/REST 请求的客户端库，用于登录、查询和提交操作。 |
| EventSource / SSE | 服务端持续向浏览器推送任务进度、模型输出和确认状态，适合 Agent 运行过程的实时展示。 |
| Nginx | 对外接收 Web 请求，托管前端静态文件，并将 API 请求转发到 FastAPI；也负责 TLS 和基础限流。 |

### 12.2 后端、Agent 与工具调用

| 技术 | 简要说明 |
|---|---|
| Python | 后端主要开发语言，适合快速实现 API、Agent 流程和第三方服务集成。 |
| FastAPI | Python Web 框架，提供 REST API、异步 I/O、依赖注入、参数校验集成和自动接口文档。 |
| Pydantic | 用类型模型校验和转换请求参数、响应数据、工具参数及 LLM 的结构化输出。 |
| LangGraph | 将 Agent 工作流表达为可控状态机，支持按节点执行、暂停确认后恢复、失败处理和流程追踪。 |
| LLM Provider Adapter | 项目内部的模型适配层，统一调用不同大模型供应商，并记录模型、用量、成本和时延。 |
| MCP Python SDK | Python 对 Model Context Protocol 的实现，用于发现和调用标准化工具。 |
| 自建 MCP Tool Server | 把日历、邮件等第三方 API 封装成受控工具，隔离外部服务差异并统一授权、参数和错误格式。 |
| Celery | 后台任务队列框架，将邮件摘要、草稿生成、同步和重试等耗时任务移出 API 请求。 |

### 12.3 数据与安全

| 技术 | 简要说明 |
|---|---|
| PostgreSQL | 主关系数据库与系统事实来源，持久保存用户、会话、任务运行、草稿、确认项、记忆和审计元数据。 |
| pgvector | PostgreSQL 的向量检索扩展；第 02 期用于保存经治理的 embedding 并执行语义相似度检索。 |
| SQLAlchemy 2 | Python ORM 和数据库访问工具，将表记录映射为 Python 对象，并支持统一查询与事务处理。 |
| Alembic | 数据库迁移工具，以版本化脚本管理建表、字段变更和数据结构升级。 |
| Redis | 内存型键值存储，用于短期上下文、工作流 checkpoint、SSE 游标、限流、幂等结果和确认令牌。 |
| OIDC / OAuth 2.0 | 登录和第三方授权标准；用于识别用户，并获取受控的日历、邮件访问权限。 |
| RBAC | 基于角色的访问控制，决定不同角色能够查看或执行哪些系统能力。 |
| NeMo Guardrails | 面向 LLM 输入输出的规则框架，用来辅助识别提示注入和约束模型输出；不替代后端权限判断。 |
| Microsoft Presidio | 识别和脱敏个人敏感信息（PII）的工具，用于保护日志、审计记录和模型输入输出。 |

### 12.4 可观测性、测试与交付

| 技术 | 简要说明 |
|---|---|
| OpenTelemetry | 统一采集并传递调用链、指标和日志关联信息，定位一次任务跨 API、模型和工具的执行路径。 |
| Prometheus | 定期采集和存储数值型运行指标，例如请求量、时延、错误率和工具失败率。 |
| Grafana | 将 Prometheus 等数据展示为仪表盘，方便观察服务健康度、性能与模型成本。 |
| Loki | 集中收集和检索结构化日志，用于按 `trace_id`、`run_id` 等标识排查问题。 |
| Alertmanager | 根据 Prometheus 告警规则聚合、去重并发送故障通知。 |
| Pytest | Python 测试框架，用于后端单元测试和业务规则测试。 |
| httpx | HTTP 客户端库，可在测试中调用 FastAPI 接口并验证 API 行为。 |
| Vitest | 前端单元测试框架，用于测试 Vue 组件、工具函数和 Pinia 状态。 |
| Playwright | 浏览器自动化测试工具，用于验证登录、任务执行、确认和邮件操作等端到端流程。 |
| Docker | 将应用及依赖打包为一致的容器镜像，减少开发、测试和部署环境差异。 |
| Docker Compose | 用一个配置文件在本地协同启动前端、后端、PostgreSQL、Redis 等多个容器。 |
| Kubernetes | 容器编排平台，后续规模增长时用于服务部署、扩缩容、健康检查和滚动更新。 |
